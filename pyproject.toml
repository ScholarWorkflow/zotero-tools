[project]
name = "zotero-tools"
version = "0.1.0"
description = "Stable command-line entry points for Zotero workflows."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
zotero-paper-tagger = "zotero_tools.tagger:main"
zotero-mcp-session = "zotero_tools.session:main"
zotero-item-export = "zotero_tools.item_export:main"

[tool.pytest.ini_options]
testpaths = ["test"]
